---
mode: "sonnet"
---
Use Sonnet (implementer).

Build the requested change with:
- deterministic commands and explicit paths
- robust error handling
- verification steps with expected output

If build/pipeline behavior changed, include required doc updates:
- PROJECT_STATUS.md
- workbook Instructions tab

Return:
1) files changed
2) run commands
3) verification results
4) short regression checklist
